export enum EStatus {
	ACTIVE = 'ACTIVE',
	COMPLETED = 'COMPLETED',
	REVOKED = 'REVOKED'
}

export enum EWhiteboardType {
	cardiacType = 1,
	eecpType = 2
}
export enum EWhiteboardStatus {
	unfinish,
	finish,
	delete
}

export enum EWhiteboardCode {
	cardiacCode = 'WHITEBOARD_SPECIAL_NOTES'
}
export enum EProvide2inject {
	whiteboard = 'whiteboard'
}
